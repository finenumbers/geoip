# Public npm for all packages. Required for Dependabot when the GitHub org
# also uses GitHub Packages — otherwise pnpm resolves @types/* from npm.pkg.github.com.
registry=https://registry.npmjs.org/
