# Установка

Руководство для оператора: развёртывание GeoIP Analytics через **Docker Compose** (CLI или **Portainer**) с опциональным NGINX Proxy Manager (NPM) для HTTPS.

## Требования

- Docker Engine и Docker Compose v2 (или Portainer CE/BE с поддержкой Compose stacks)
- Доступ к личному кабинету ГРЧЦ GeoIP (email и пароль — у каждого пользователя свои)
- Опционально: домен и NGINX Proxy Manager для HTTPS и Access List

## Способы установки

| Способ | Compose / команда | Когда использовать |
|--------|-------------------|-------------------|
| **[Portainer](PORTAINER.md)** | `docker-compose.portainer.yml` | **Production на сервере** — один self-contained файл, GHCR images |
| [Docker Compose (CLI)](#вариант-1-docker-compose-cli) | `docker-compose.yml` + `docker-compose.prod.yml` | SSH, локальный prod-like стенд, bind mounts на `infra/` |
| [NPM (периметр)](#nginx-proxy-manager) | — | HTTPS и Access List — см. [NGINX-PROXY-MANAGER.md](NGINX-PROXY-MANAGER.md) |

Файл `.env` **не обязателен** для Docker: в compose заданы defaults `POSTGRES_*=geoip`, образы GHCR `:latest`. `CONFIG_MASTER_KEY` **не** прописан в compose — при необходимости задайте в Portainer env или `.env` (64 hex); иначе ключ автогенерируется в volume `config_data`.

---

## Вариант 1: Docker Compose (CLI)

### 1. Клонировать репозиторий

```bash
git clone https://github.com/finenumbers/geoip.git
cd geoip
```

### 2. (Опционально) Локальная разработка

```bash
cp .env.example .env
# Только для npm run dev / psql — Docker Compose .env не требует
```

Минимальный bootstrap — только инфраструктура. Секреты ГРЧЦ, API keys и Google Maps задаются в Admin UI.

### 3. Запустить production stack

С готовыми образами GHCR (рекомендуется на сервере):

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

Локальная сборка образов (нужен полный git-клон):

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.build.yml build
docker compose -f docker-compose.yml -f docker-compose.prod.yml -f docker-compose.build.yml up -d
```

### 4. Проверить контейнеры

```bash
docker compose ps
curl -s http://localhost:8080/api/v1/health
curl -s http://localhost:8080/api/v1/public/setup-checklist
```

### 5. Первичная настройка

Откройте `http://<хост>:8080/admin/setup`, создайте admin, затем `/admin/setup-api-key`. Дальше — [checklist первого запуска](#первый-запуск-checklist).

### Обновление (CLI)

```bash
cd geoip
git pull
docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

Для локальных образов добавьте `-f docker-compose.build.yml` и `build` вместо `pull`.

Volume `config_data` сохраняет настройки между redeploy. Если задан `CONFIG_MASTER_KEY`, secrets останутся расшифровываемыми.

### Backup volume `config_data`

Volume содержит операционную конфигурацию Admin: `settings.json`, зашифрованный `secrets.enc`, опционально `proxy.env` и `.master-key` (если `CONFIG_MASTER_KEY` не задан в env).

**Что сохранить отдельно:** значение `CONFIG_MASTER_KEY` из Portainer / `.env` (если вы его задавали). Без master key и без `.master-key` в backup расшифровать `secrets.enc` нельзя.

**Создать архив (CLI, имя volume может отличаться — проверьте `docker volume ls`):**

```bash
mkdir -p backups
docker run --rm \
  -v geoip_config_data:/data:ro \
  -v "$(pwd)/backups:/backup" \
  alpine tar czf "/backup/config_data-$(date +%Y%m%d).tar.gz" -C /data .
```

**Восстановление:** остановите stack, распакуйте архив в volume, запустите stack с тем же `CONFIG_MASTER_KEY` (или с тем же `.master-key`, что был в backup).

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml down
docker run --rm \
  -v geoip_config_data:/data \
  -v "$(pwd)/backups:/backup:ro" \
  alpine sh -c 'cd /data && tar xzf /backup/config_data-YYYYMMDD.tar.gz'
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

Для Portainer: **Volumes → config_data → Browse / Backup** (если доступно) или тот же `docker run` с именем volume из stack.

См. также [БЕЗОПАСНОСТЬ.md](БЕЗОПАСНОСТЬ.md).

### Остановка и удаление

```bash
# Остановить без удаления данных
docker compose -f docker-compose.yml -f docker-compose.prod.yml down

# Удалить контейнеры и volumes (данные БД и config будут потеряны!)
docker compose -f docker-compose.yml -f docker-compose.prod.yml down -v
```

---

## Вариант 2: Portainer

> **Полная инструкция:** **[PORTAINER.md](PORTAINER.md)**

| Поле | Значение |
|------|----------|
| Compose path | **`docker-compose.portainer.yml`** (не `docker-compose.yml`) |
| Environment | опционально [`stack.env.example`](../infra/portainer/stack.env.example) — в основном `CONFIG_MASTER_KEY` |

Кратко:

1. **Portainer → Stacks → Add stack → Repository**
2. Compose path: **`docker-compose.portainer.yml`**
3. Deploy → откройте `http://<хост>:8080/admin/setup`
4. [Checklist первого запуска](#первый-запуск-checklist)

Файл `infra/portainer/stack.compose.yml` — **legacy** (include + build на хосте); для Portainer используйте **`docker-compose.portainer.yml`** с inline configs и без bind mounts.

См. также [`infra/portainer/README.md`](../infra/portainer/README.md).

---

## Первый запуск (checklist)

После любого способа установки база данных **пустая** — это ожидаемо. На Dashboard и в Admin → Обзор отображается checklist:

1. **Admin** — `/admin/setup`: логин и пароль локального администратора
2. **API-ключ External IP Lookup** — `/admin/setup-api-key`: Generate → Save (обязательно)
3. **ГРЧЦ** — Admin → ГРЧЦ / Import: email и пароль ЛК, сохранить, «Проверить ГРЧЦ»
4. **Импортировать датасет** — Admin → Обзор → «Импортировать датасет» (или дождаться cron **10:00 MSK**)
5. **Google Maps** (опционально) — Admin → Интеграции

Пока import не завершён успешно, `/api/v1/ready` возвращает `not_ready` — это нормально.

**Не используйте** `seed:fixture` в production — только для локальной разработки и CI.

---

## Состав stack (Docker Compose)

| Сервис | Назначение |
|--------|------------|
| `postgres` | База данных |
| `pgbouncer` | Connection pooling |
| `api` | HTTP API + migrations on startup |
| `import` | Import worker + cron |
| `export` | Export worker |
| `web` | Nginx + SPA (порт **8080** на хосте в prod) |

## Volumes

| Volume | Назначение |
|--------|------------|
| `pg_data` | PostgreSQL |
| `config_data` | `settings.json`, `secrets.enc`, `proxy.env`, `.master-key` (если master key автогенерирован) |
| `import_data` | Временные файлы import worker |
| `export_data` | CSV-экспорты |

---

## NGINX Proxy Manager

Подробная инструкция: **[NGINX-PROXY-MANAGER.md](NGINX-PROXY-MANAGER.md)**

Кратко (CLI и Portainer):

1. **Proxy Host:** домен → `http://<docker-host>:8080`, схема **HTTP**
2. **SSL:** Let's Encrypt на NPM (не на контейнере)
3. **Access List:** ограничьте доступ к UI и `/admin`
4. Не публикуйте порты API (`3000`) и Postgres на хост — prod overlay оставляет только `:8080`

После настройки NPM открывайте `https://<ваш-домен>/admin/setup`.

---

## Переопределение defaults при деплое

| Параметр | Где | Зачем |
|----------|-----|-------|
| `POSTGRES_*` | правка `docker-compose*.yml` (+ pgbouncer userlist для dev) | Сильный пароль БД |
| `CONFIG_MASTER_KEY` | Portainer env или `.env` | 64 hex — стабильное шифрование `secrets.enc`; не задан в compose |
| Image tag | замена `:latest` в compose | Pin версии GHCR |

Операционные секреты (ГРЧЦ, API keys, Google Maps) — только через **Admin UI**.

---

## Восстановление данных

Автобэкап PostgreSQL не используется. При потере или повреждении данных выполните **повторный import** датасета из ГРЧЦ (Admin → Обзор).

---

## См. также

- [PORTAINER.md](PORTAINER.md) — **деплой в Portainer (Stacks)**
- [ADMIN.md](ADMIN.md) — разделы Admin UI
- [FAQ.md](FAQ.md) — типичные проблемы
- [БЕЗОПАСНОСТЬ.md](БЕЗОПАСНОСТЬ.md) — секреты и что не коммитить
- [NGINX-PROXY-MANAGER.md](NGINX-PROXY-MANAGER.md) — HTTPS через NPM
- [АРХИТЕКТУРА.md](АРХИТЕКТУРА.md) — архитектура системы
- [infra/portainer/README.md](../infra/portainer/README.md) — краткий чеклист Portainer

---

**Проект:** [github.com/finenumbers/geoip](https://github.com/finenumbers/geoip)  
**Разработчик:** [Finenumbers](https://finenumbers.com) · apps@finenumbers.com
