# Copy into Portainer Stack → Environment variables (or repo .env on the host)

POSTGRES_USER=geoip
POSTGRES_PASSWORD=ваш-сильный-пароль-postgres
POSTGRES_DB=geoip

# Bootstrap only — operational secrets live in Admin UI (/admin) after first setup
CONFIG_MASTER_KEY=

BACKUP_INTERVAL_SECONDS=86400
